export const metadata = {
  title: 'Case Studies',
  description: 'See how Proxi helps lawyers, doctors, property agents, and professionals across Malaysia reclaim their time.',
};

export default function Layout({ children }) {
  return children;
}
