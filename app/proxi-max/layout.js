export const metadata = {
  title: 'Proxi Max — RM499/mo',
  description: 'Your PA on superhero mode. Admin, custom workflows, automation, and advanced integrations — one operator.',
};

export default function Layout({ children }) {
  return children;
}
