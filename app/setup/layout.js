export const metadata = {
  title: 'Setup Fee Explained',
  description: 'One-time setup fee for Proxi (RM500) and Proxi Max (RM2,000). Here is what you get.',
};

export default function Layout({ children }) {
  return children;
}
