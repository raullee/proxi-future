export const metadata = {
  title: 'Human PA vs AI PA',
  description: 'A side-by-side comparison of hiring a human personal assistant versus using Proxi.',
};

export default function Layout({ children }) {
  return children;
}
