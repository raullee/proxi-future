export const metadata = {
  title: 'Meet Naoise — Case Study',
  description: 'How our first Digital Engagement Officer produced 11 blog posts in week one and launched 3 social accounts.',
};

export default function Layout({ children }) {
  return children;
}
