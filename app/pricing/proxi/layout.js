export const metadata = {
  title: 'Proxi Plan — RM199/mo',
  description: 'Your dedicated AI personal assistant. Email, calendar, follow-ups, and admin — handled 24/7 for RM199/month.',
};

export default function Layout({ children }) {
  return children;
}
