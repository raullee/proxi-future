export const metadata = {
  title: 'Free Plan',
  description: 'Build your own AI PA with OpenClaw — the open-source framework that powers Proxi. Self-hosted, full control.',
};

export default function Layout({ children }) {
  return children;
}
