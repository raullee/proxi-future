export const metadata = {
  title: 'Enterprise',
  description: 'Custom AI operations for your team. We build the agents, you run the business.',
};

export default function Layout({ children }) {
  return children;
}
