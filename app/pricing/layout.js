export const metadata = {
  title: 'Pricing',
  description: 'Compare Proxi plans — Free, Proxi (RM199/mo), Proxi Max (RM499/mo), and Enterprise. Find the right AI personal assistant for your needs.',
};

export default function Layout({ children }) {
  return children;
}
