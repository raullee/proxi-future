export const metadata = {
  title: 'Blog',
  description: 'Tips, insights, and stories about AI personal assistants, productivity, and the future of work.',
};

export default function Layout({ children }) {
  return children;
}
